// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you later switch to <Image />, keep this or configure domains.
  // images: { unoptimized: true },
};

export default nextConfig;
